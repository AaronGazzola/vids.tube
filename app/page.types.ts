export interface EditorLayoutConfig {
  showSidebar: boolean;
  sidebarWidth: number;
  mainAreaHeight: number;
}

export interface ResponsiveBreakpoint {
  mobile: boolean;
  tablet: boolean;
  desktop: boolean;
}
